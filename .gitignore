# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local data and generated output
/data/
/output/

# Caches at any depth
cache/
caches/
.cache/
*_cache/
*-cache/

# wrangler cached the account id and the account name (an email address) here; retired 2026-09-05
.wrangler/
# the Vercel CLI's project link and the pulled environment files (`vercel pull`)
.vercel/
.env*.local
web/public/data/*
!web/public/data/.gitkeep

# Web
node_modules/
web/dist/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# session scratch (kept out of the repository)
.tmp/
# the working record; it lives in the private folder alongside the commercial material
docs/development-log.md
